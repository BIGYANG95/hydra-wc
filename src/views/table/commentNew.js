const user={
  name:'commentNew',
  table:{
    ids:'varchar(20) primary key',
    reply:'varchar(200)',
    text:'varchar(200)',
    blogId:'varchar(200)',
    userId:'varchar(200)'
  }
}

export default user
