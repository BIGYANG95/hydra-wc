const user={
    name:'comment',
    table:{
        ids:'varchar(20) primary key',
        name: "varchar(20)",
        phone: "varchar(20)",
        text: "varchar(20)",
    }
}

export default user
